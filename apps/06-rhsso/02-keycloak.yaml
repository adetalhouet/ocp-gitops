---
apiVersion: keycloak.org/v1alpha1
kind: Keycloak
metadata:
  name: keycloak
  namespace: openshift-sso
  labels:
    app: sso
spec:
  externalAccess:
    enabled: true
  instances: 1
  storageClassName: ocs-storagecluster-ceph-rbd